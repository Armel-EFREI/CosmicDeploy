apiVersion: v1
kind: Service
metadata:
  name: cosmicdeploy
  labels:
    app: cosmicdeploy
spec:
  type: NodePort
  selector:
    app: cosmicdeploy
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30080
